/**
 * external library of filter
 */
import { Image } from "./solution";

export class Caramel {
  init() {
    // suppose we need to init to use render method
  }

  render(image: Image) {
    console.log("caramel filter -- third party");
  }
}
